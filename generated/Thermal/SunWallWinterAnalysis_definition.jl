### DO NOT EDIT THIS FILE
### This file is auto-generated by the Dyad command-line compiler.
### If you edit this code it is likely to get overwritten.
### Instead, update the Dyad source code and regenerate this file


using DyadInterface
using DyadInterface: ODEAlg, DEVerbosity, OptimizationLevel
using ModelingToolkit: SymbolicT, toggle_namespacing
using DyadInterface: AbstractTransientAnalysisSpec, TransientAnalysisSpec
@kwdef mutable struct SunWallWinterAnalysisSpec <: AbstractTransientAnalysisSpec
  name::Symbol = :SunWallWinterAnalysis
  var"alg"::ODEAlg.Type = ODEAlg.Auto()
  var"start"::Float64 = 0
  var"stop"::Float64 = 86400.0
  var"abstol"::Float64 = 0.000001
  var"reltol"::Float64 = 0.000001
  var"saveat"::Float64 = 0
  var"dtmax"::Float64 = 0
  var"tstops"::Array{Float64, 1} = []
  var"automatic_discontinuity_detection"::Bool = false
  var"optimize"::OptimizationLevel.Type = OptimizationLevel.Aggressive()
  var"progress"::Bool = true
  var"respecialize"::Bool = false
  var"verbose"::DEVerbosity.Type = DEVerbosity.Standard()
  var"log_file"::String = ""
  # One day on three external walls at 43.4°N: a south wall, a north wall, and a south wall
  # under an overhang. `day_of_year` picks the season.
  # 
  # Each wall is a 200 mm concrete slab (`PlateTransient`) with `ConvRadSunWall` on its outer
  # face and a 20 °C room on its inner face. Ambient air is 25 °C, the clear sky 20 K colder,
  # and a 3 m/s breeze blows due east — square-on to every wall normal, so all three see the
  # same film coefficient and solar gain is the only thing that differs between them.
  # 
  # `south` carries a glazed fraction (`tau_trans = 0.1`), so its `port_Transmission` is the
  # only one with flux through it; the other two are connected to the same room but pass
  # nothing.
  # 
  # Solar noon is at 13.55 h on this clock: the site is UTC+1 at 8.24° west.
  var"model"::Union{Nothing, System} = DyadShip.Thermal.SunWallDay(; name=:SunWallDay)
end

function DyadInterface.run_analysis(spec::SunWallWinterAnalysisSpec)
  overrides = Dict{SymbolicT, SymbolicT}()
  no_namespace_model = toggle_namespacing(spec.model, false)
  push!(overrides, no_namespace_model.day_of_year => 355)
  base_spec = TransientAnalysisSpec(;
    name=:TransientAnalysis, overrides, alg=spec.alg, start=spec.start, stop=spec.stop, abstol=spec.abstol, reltol=spec.reltol, saveat=spec.saveat, dtmax=spec.dtmax, tstops=spec.tstops, automatic_discontinuity_detection=spec.automatic_discontinuity_detection, optimize=spec.optimize, progress=spec.progress, respecialize=spec.respecialize, verbose=spec.verbose, log_file=spec.log_file, model=spec.model
  )
  run_analysis(base_spec)
end

SunWallWinterAnalysis(;kwargs...) = run_analysis(SunWallWinterAnalysisSpec(;kwargs...))
export SunWallWinterAnalysis, SunWallWinterAnalysisSpec
