### DO NOT EDIT THIS FILE
### This file is auto-generated by the Dyad command-line compiler.
### If you edit this code it is likely to get overwritten.
### Instead, update the Dyad source code and regenerate this file


using DyadInterface
using DyadInterface: ODEAlg, DEVerbosity, OptimizationLevel
using ModelingToolkit: SymbolicT, toggle_namespacing
using DyadInterface: AbstractTransientAnalysisSpec, TransientAnalysisSpec
@kwdef mutable struct ConvectionSpeedSweepAnalysisSpec <: AbstractTransientAnalysisSpec
  name::Symbol = :ConvectionSpeedSweepAnalysis
  var"alg"::ODEAlg.Type = ODEAlg.Auto()
  var"start"::Float64 = 0
  var"stop"::Float64 = 200.0
  var"abstol"::Float64 = 0.000001
  var"reltol"::Float64 = 0.000001
  var"saveat"::Float64 = 0
  var"dtmax"::Float64 = 0
  var"tstops"::Array{Float64, 1} = []
  var"automatic_discontinuity_detection"::Bool = false
  var"optimize"::OptimizationLevel.Type = OptimizationLevel.Aggressive()
  var"progress"::Bool = true
  var"respecialize"::Bool = false
  var"verbose"::DEVerbosity.Type = DEVerbosity.Standard()
  var"log_file"::String = ""
  # Speed sweep across the three flow-driven convection factors, with a cylinder cooling
  # through its own coefficient.
  # 
  # The wind ramps from rest to 20 m/s over the run while a hot body sits in air at 25 °C, so
  # each element's `h` is swept across the branches of its correlation in one transient:
  # 
  # - `cyl` covers the natural-convection plateau at rest, the Nakai–Okazaki slug-flow branch
  #   and both Churchill–Bernstein forms. It carries the only thermal mass in the model, so it
  #   also checks the element inside a real cooling loop rather than between two ideal sources.
  #   Its geometry is the ceramic disk resistor of upstream's `CeramicDiskResistor_Test`
  #   (D = 151 mm, L = 889 mm).
  # - `windward` and `leeward` are the same wall, facing south and north, in a wind blowing
  #   due north. They share every parameter, so `leeward.h` should be exactly half
  #   `windward.h` throughout — the windward/leeward factor is the only thing that differs.
  # - `plate` sweeps the turbulent flat-plate correlation. Below Re ≈ 5·10⁵ it is out of the
  #   range the correlation is fitted over; `plate.Re` shows where the run crosses it.
  # 
  # The hot side is held at 150 °C except for `cyl`, which cools from there.
  var"model"::Union{Nothing, System} = DyadShip.Thermal.ConvectionSpeedSweep(; name=:ConvectionSpeedSweep)
end

function DyadInterface.run_analysis(spec::ConvectionSpeedSweepAnalysisSpec)
  overrides = Dict{SymbolicT, SymbolicT}()
  no_namespace_model = toggle_namespacing(spec.model, false)
  
  base_spec = TransientAnalysisSpec(;
    name=:TransientAnalysis, overrides, alg=spec.alg, start=spec.start, stop=spec.stop, abstol=spec.abstol, reltol=spec.reltol, saveat=spec.saveat, dtmax=spec.dtmax, tstops=spec.tstops, automatic_discontinuity_detection=spec.automatic_discontinuity_detection, optimize=spec.optimize, progress=spec.progress, respecialize=spec.respecialize, verbose=spec.verbose, log_file=spec.log_file, model=spec.model
  )
  run_analysis(base_spec)
end

ConvectionSpeedSweepAnalysis(;kwargs...) = run_analysis(ConvectionSpeedSweepAnalysisSpec(;kwargs...))
export ConvectionSpeedSweepAnalysis, ConvectionSpeedSweepAnalysisSpec
