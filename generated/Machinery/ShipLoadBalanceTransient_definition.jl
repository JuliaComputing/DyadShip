### DO NOT EDIT THIS FILE
### This file is auto-generated by the Dyad command-line compiler.
### If you edit this code it is likely to get overwritten.
### Instead, update the Dyad source code and regenerate this file


using DyadInterface
using DyadInterface: ODEAlg, DEVerbosity, OptimizationLevel
using ModelingToolkit: SymbolicT, toggle_namespacing
using DyadInterface: AbstractTransientAnalysisSpec, TransientAnalysisSpec
@kwdef mutable struct ShipLoadBalanceTransientSpec <: AbstractTransientAnalysisSpec
  name::Symbol = :ShipLoadBalanceTransient
  var"alg"::ODEAlg.Type = ODEAlg.Auto()
  var"start"::Float64 = 0
  var"stop"::Float64 = 21600.0
  var"abstol"::Float64 = 0.000001
  var"reltol"::Float64 = 0.000001
  var"saveat"::Float64 = 0
  var"dtmax"::Float64 = 0
  var"tstops"::Array{Float64, 1} = []
  var"automatic_discontinuity_detection"::Bool = false
  var"optimize"::OptimizationLevel.Type = OptimizationLevel.Aggressive()
  var"progress"::Bool = true
  var"respecialize"::Bool = false
  var"verbose"::DEVerbosity.Type = DEVerbosity.Standard()
  var"log_file"::String = ""
  # Six hours of a small ship's electrical load: six consumers on independent random
  # schedules, and the bus load they add up to.
  # 
  # The consumer list is the shape upstream's `Balance` uses — a few large intermittent
  # machines (a 37 kW compressor, a 15 kW pump) among smaller continuous ones — at a 30 s
  # scheduler tick, which resolves the shortest mean period here (400 s) about thirteen
  # times over. Every periodic clock forces a solver step per tick, so the tick rate is what
  # sets the cost of a long EPLA run: six schedulers over six hours is already 4320 ticks. What an EPLA wants out of it is the running average against
  # the installed total, which is the diversity factor.
  # 
  # The schedule is reproducible: the same `seed_base` and `dt` give the same switching
  # sequence, so a change in the load list can be compared against a baseline.
  var"model"::Union{Nothing, System} = DyadShip.Machinery.ShipLoadBalance(; name=:ShipLoadBalance)
end

function DyadInterface.run_analysis(spec::ShipLoadBalanceTransientSpec)
  overrides = Dict{SymbolicT, SymbolicT}()
  no_namespace_model = toggle_namespacing(spec.model, false)
  
  base_spec = TransientAnalysisSpec(;
    name=:TransientAnalysis, overrides, alg=spec.alg, start=spec.start, stop=spec.stop, abstol=spec.abstol, reltol=spec.reltol, saveat=spec.saveat, dtmax=spec.dtmax, tstops=spec.tstops, automatic_discontinuity_detection=spec.automatic_discontinuity_detection, optimize=spec.optimize, progress=spec.progress, respecialize=spec.respecialize, verbose=spec.verbose, log_file=spec.log_file, model=spec.model
  )
  run_analysis(base_spec)
end

ShipLoadBalanceTransient(;kwargs...) = run_analysis(ShipLoadBalanceTransientSpec(;kwargs...))
export ShipLoadBalanceTransient, ShipLoadBalanceTransientSpec
