### DO NOT EDIT THIS FILE
### This file is auto-generated by the Dyad command-line compiler.
### If you edit this code it is likely to get overwritten.
### Instead, update the Dyad source code and regenerate this file


import Moshi as __Ext__Moshi

@doc Markdown.doc"""
   CableBreakLatch(; name, RotureStrength)

Tension-only elastic cable between two 3D frames, with a latching break.

Port of `ShipSIM.SubComponents.Cable` on `MultibodyComponents.PartialLineForce`:
the cable pulls its two frames together along the line joining them with
`f = k (s - SetLength) + d der(s)` when it is longer than its unstretched
length `SetLength` (an input, so a winch can pay it out), and carries no
force when slack. It transmits no torque; set `fixed_rotation_at_frame_b =
true` when a body without its own orientation states hangs from `frame_b`.

The upstream break event (`when k (s - SetLength) > RotureStrength`) is a
clocked latch: the elastic tension is sampled every `dt_check` seconds on a
`DiscreteComponents.PeriodicClock`, and the `CableBreakLatch` state `intact`
drops to zero the first time the sample exceeds `RotureStrength` and never
recovers. The force is multiplied by the held state, so a parted cable goes
slack within one sampling period.

## Parameters:

| Name         | Description                         | Units  |   Default value |
| ------------ | ----------------------------------- | ------ | --------------- |
| `RotureStrength`         |                          | N  |   1e12 |

## Connectors

 * `f` - This connector represents a real signal as an input to a component ([`RealInput`](@ref))
 * `intact` - This connector represents a real signal as an output from a component ([`RealOutput`](@ref))
"""
@component function CableBreakLatch(; name = nothing, RotureStrength=Float64(1000000000000.0), kwargs...)
  isnothing(name) && throw(ArgumentError("""
    The `name` keyword must be provided. Please consider using the `@named` macro,
    like so:
  
    @named model = CableBreakLatch()
  """))

  __overrides = __build_overrides(kwargs)
  __params = Symbolics.SymbolicT[]
  __vars = Symbolics.SymbolicT[]
  __systems = System[]
  __guesses = Dict{Symbolics.SymbolicT, Symbolics.SymbolicT}()
  __initial_conditions = Dict{Symbolics.SymbolicT, Symbolics.SymbolicT}()
  __initialization_eqs = Equation[]
  __eqs = Equation[]
  __bindings = Dict{Symbolics.SymbolicT, Symbolics.SymbolicT}()

  ### Structural Parameters (functions)

  ### Structural Parameters (Final)

  ### Path Parameters (functions)

  ### Path Parameters (non-final)

  ### Final Parameters (declarations)

  ### Deferred assignment (default values that depend on final parameters)

  ### Symbolic Parameters
  __local__RotureStrength = RotureStrength
  append!(__params, @parameters (RotureStrength::Real))
  __initial_conditions[RotureStrength] = __local__RotureStrength

  ### Final Parameters (assignments)

  ### Final Path Parameters
  append!(__vars, @variables (f(t)::Real), [input = true])
  append!(__vars, @variables (intact(t)::Real), [output = true])

  ### Variables (declarations)

  ### Variables (assignments)

  ### Constants
  __constants = Any[]

  ### Components

  ### Check there are no unmatched overrides
  isempty(__overrides) || throw(ArgumentError("overrides: [$(join(keys(__overrides), ", "))] don't match names found in model. These names may exist in the model but could have been conditionally excluded."))

  ### Guesses

  ### Initialization Equations
  push!(__initialization_eqs, intact(ShiftIndex() -1) ~ 1)

  ### Assertions
  __assertions = []

  ### Equations
  push!(__eqs, intact ~ ifelse(intact(ShiftIndex() -1) > 0.5, ifelse(f > RotureStrength, 0, 1), 0))

  # Return completely constructed System
  return System(__eqs, t, __vars, __params; systems=__systems, initial_conditions=__initial_conditions, guesses=__guesses, name, initialization_eqs=__initialization_eqs, bindings=__bindings, assertions=__assertions)
end
export CableBreakLatch
